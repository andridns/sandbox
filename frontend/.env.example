# Expense Tracker Frontend - Environment Variables Example
# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control

# Backend API URL
# For local development, use the default /api/v1 (proxied by Vite)
# For production, set to your backend URL + /api/v1
# Example: https://your-backend.railway.app/api/v1
VITE_API_URL=/api/v1

# Google OAuth Client ID (Optional)
# Get from Google Cloud Console: https://console.cloud.google.com/
# Required for Google Sign-In functionality
# Must match the GOOGLE_CLIENT_ID in backend .env
VITE_GOOGLE_CLIENT_ID=
